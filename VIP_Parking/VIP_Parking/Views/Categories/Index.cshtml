@model IEnumerable<VIP_Parking.Models.Database.Category>

@{
    ViewBag.Title = "VIP Categories";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>VIP Categories</h2>
<p style="margin-bottom:25px">
    @Html.ActionLink("Back to Reservations", "Index", "Reservations")
</p>
<p>
    @Html.ActionLink("Create New VIP Category", "Create", null, new { @class = "link_button purple" })
</p>
<table class="table">
    <tr>
        <th>
            VIP Categories
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Category_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Category_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Category_ID })
        </td>
    </tr>
}

</table>

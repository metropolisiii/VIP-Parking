@model IEnumerable<VIP_Parking.Models.Database.Requester>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Users</h2>

<ul id="users_list">
    @foreach (var item in Model)
    {
        <li>
         @Html.ActionLink(item.Firstname+" "+item.Lastname, "Edit", new { id = item.Requester_ID })
        </li>
    }
</ul>

